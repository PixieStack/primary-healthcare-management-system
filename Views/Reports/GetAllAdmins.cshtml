
<!DOCTYPE html>
<html>
<head>
    <title>All Adminstrators</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f5f5f5;
        }

        .centered-heading {
            text-align: center;
            padding: 20px;
            margin: 0;
            background-color: #f5f5f5;
            color: #36517E;
        }

        .container {
            max-width: 100%;
            padding: 20px;
        }

        .table-container {
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        th, td {
            padding: 10px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        .btn {
            padding: 8px 16px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            color: #fff;
        }

        .btn-primary {
            background-color: #007bff;
        }


        .btn {
            padding: 6px 12px;
        }

        }
    </style>
</head>
<body>
    <h1 class="centered-heading">All Adminstrators Report</h1>

    <div class="container">
        <!-- ... (same as before) -->

        <table border="1">
            <thead>
                <tr>
                    <th>Full Name</th>
                    <th>Gender</th>
                    <th>Citizenship</th>
                    <th>Phone Number</th>
                    <th>Email</th>
                    <th>Street Address</th>
                    <th>Suburb</th>
                    <th>City</th>
                    <th>Province</th>
                    <th>Postal Code</th>
                    <!-- Add more headers for additional properties -->
                </tr>
            </thead>
            <tbody>
                @if (ViewBag.Admins != null)
                {
                    @foreach (var admin in ViewBag.Admins)
                    {
                        <tr>
                            <td>@admin.FullName</td>
                            <td>@admin.Gender</td>
                            <td>@admin.Citizenship</td>
                            <td>@admin.Phone_Number</td>
                            <td>@admin.Email</td>
                            <td>@admin.Street_Address</td>
                            <td>@admin.Surburb</td>
                            <td>@admin.City</td>
                            <td>@admin.Province</td>
                            <td>@admin.Postal_Code</td>
                            <!-- Add more cells for additional properties -->
                        </tr>
                    }
                }

            </tbody>
        </table>
    </div>

    <!-- ... (same as before) -->

</body>
</html>
